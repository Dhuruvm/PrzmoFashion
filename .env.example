# PRZMO Environment Variables

# SMTP Server Configuration (Replace SendGrid)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-or-regular-password
SMTP_SECURE=false

# Optional: Pre-configured API key for SMTP
SMTP_API_KEY=your-generated-api-key
SMTP_DOMAIN=przmo.app

# JWT Secret for API key generation (REQUIRED - Change in production!)
JWT_SECRET=your-super-secure-jwt-secret-change-this-in-production

# Database Configuration (if using PostgreSQL)
DATABASE_URL=your-database-connection-string

# Application Configuration
NODE_ENV=development
PORT=5000

# Security Configuration
SESSION_SECRET=your-session-secret

# Email Configuration
DEFAULT_FROM_EMAIL=noreply@przmo.app
DEFAULT_FROM_NAME=PRZMO Athletic

# Rate Limiting Configuration (Optional)
SMTP_RATE_LIMIT_HOUR=100
SMTP_RATE_LIMIT_MINUTE=10

# Logging Configuration
LOG_LEVEL=info
LOG_TO_FILE=false